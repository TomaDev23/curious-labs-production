# Agent mission · integrate the CuriousLabs AI consultation page

## Mission

Use this v0.3 HTML package as the visual and structural reference for a new AI consultation page and one homepage invitation on the CuriousLabs website. Implement it in the actual codebase. Keep the change narrow. This is a consultation-page task, not a site redesign, architecture migration, or repository-cleanup campaign.

Do not start by recreating a screenshot as a single image or by building a new site shell. Read the current repository's local instructions and actual integration points first.

## Read order

1. `README.md` — authority, file roles, and what is not finished.
2. `CuriousLabs_AI_Consultation_v0.3.html` — open the page, use Homepage panel and Build notes, and inspect desktop/mobile behavior.
3. `docs/CONTENT_MAP.md` — IDs, copy coverage, actions, and missing content.
4. `docs/IMPLEMENTATION_PLAN.md` — phased integration and acceptance.
5. `docs/DESIGN_SYSTEM.md` and `docs/QA_REPORT.md` — styling ownership, actual checks, and limitations.
6. The supplied source documents and PNGs in `references/` as needed for exact comparison.

The v0.2 chart is the latest supplied text/shape source; v0.1 supplies fuller boundaries. Screenshots guide the visual treatment. Do not import their generic cycle, booking implication, or unsupported promotional wording as new service authority. The underlying Discovery Dossier was not independently read by the reference author.

## First checkpoint: establish the real seam

Identify the live route registration, active homepage, navbar/footer, background owner, styling conventions, and exact MoonSignal promotional-panel insertion point. Record the branch/commit and actual files read. The older uploaded file inventory is not current evidence and does not authorize assumed imports.

Propose/confirm the route name; `/ai-consultation` is only a suggested default. Reuse the existing shell. Keep the current moon-led homepage opening; insert H1 immediately before the MoonSignal promotional panel. Leave the remaining MoonSignal and AEGIS content unchanged.

Do not add a dependency, contract initialization, global provider, background canvas, or new 3D engine unless the current code and a concrete requirement justify it. This reference needs none of those additions.

## Implement the actual page

Preserve the main sequence:

`D1 Offer → D2 Audiences → D3 Contribution → D4 Person → D5 Approach → D6 Evidence → D7 Engagement → D8 Contact`

Keep R1–R4 inside D5 and make each independently expandable. Preserve the stable anchors in `CONTENT_MAP.md`. Keep both audiences visible without tabs or forced selection. Keep all important service meaning outside the expansions. Use real HTML text and accessible controls, not screenshot crops.

The prototype's `cl-*` classes and `--cl-*` tokens are scoped for this page. Translate them into the project's convention without leaking styles onto existing pages. Use the existing font and shared branding. Do not ship the demo shell, fake operational telemetry, source documents, reference toolbar, or build notes.

Port the small interactions into the framework instead of applying `js/consultation.js` imperatively over framework-owned DOM. Preserve deep-link opening, focus restoration, the direct contact shortcut, and clipboard fallback. The optional SVG illustration must use unique instance IDs and must not become an extra application-wide rendering system.

## Content that is safe to draft, but not self-approve

The source-supported wording is present in the reference. Do not silently rewrite a tailored service into a standard AI course, fixed package, coding lesson, development service, or a team consultancy. “I” means personal delivery. The source's residence duration refers to living in Cambodia, not AI experience; verify its public wording before launch.

Do not infer or define Maestro, Orchestrator mode, LEGIT, planning principles, or the owner's theory. The supported short reading paragraphs can be approved as short paragraphs; missing full articles do not justify invented explanations.

The free discussion, meeting-summary email, proposal/quote, and paid report are distinct. Full preparation follows confirmation and the agreed contract/payment/deposit arrangements. Do not create prices, deadlines, fixed meeting lengths, deposit percentages, automatic email sending, or appointment slots.

## Missing content: handle it honestly

The two evidence cards are marked reserves. Replace them only with selected own-work artifacts, approved redactions, meaningful captions, explicit limits, and real alt text. No client case studies, testimonials, returns, fabricated matrices, or decorative diagrams presented as proof. Omit an unready slot; omit D6 entirely if neither is ready.

Public identity/title, final biography, and any portrait or affiliation require owner approval. A text-led introduction is acceptable without a portrait. Do not use a stock person or infer public identity from unrelated data.

Telegram, WhatsApp, and email are deliberately blank. Use approved public destinations only. The CTA leads to contact choices, not a scheduler. Email opens a composer; it never sends automatically. QR codes are optional follow-on assets after destinations are verified. Facebook is absent until its real destination exists. No required form, account, upload, checkout, mailing list, or analytics funnel should be introduced.

For release, provide at least one verified contact route and hide other unconfigured channels. The reference shows all three slots to explain the intended shape; it is not permission to publish dead controls.

## Acceptance

Demonstrate the route and H1 placement in the real application. Test narrow phone, tablet, and desktop widths, open reading rows, deep-linked R4, visible keyboard focus, Escape/close behavior, fixed-header anchor offsets, reduced motion, selectable email, and clipboard failure. Use actual approved contact destinations for final link verification without sending messages.

Run the project's build and appropriate existing tests. Verify no duplicated shell/background, broken route refresh, horizontal overflow, leaked global styles, stale source copy, debug UI, or unrelated product regression. The reference's Chromium checks are not a substitute for app integration, Safari/Firefox, physical-device, or assistive-technology checks.

## Report back

Provide the current source cut, files changed, route/insertion decisions, representative desktop/mobile screenshots, build/test results, and the remaining content/publication blockers. Distinguish implemented structure, approved public content, and unfinished input. Keep an uncomplicated rollback path for the route and homepage panel.

Do not report completion of publication while approval gates are still open. Do not expand the mission into unrelated cleanup. Do not change the existing shell or product pages to make this page easier to build.
